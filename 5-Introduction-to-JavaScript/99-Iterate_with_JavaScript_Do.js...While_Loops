/* Do/while loops are just like while loops except that they are guaranteed to
execute once before checking the while condition. */

// Setup
var myArray = [];
var i = 10;

// Only change code below this line.
do {
  myArray.push(i);
  i++;
} while (i < 5);
