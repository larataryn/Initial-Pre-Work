/* You can use a comparison operation in the return statement to return true or
false from the function. */

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
