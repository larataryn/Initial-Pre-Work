/* The inequality (!=) operator returns true if two values are not equal, or
false if the values are equal. */

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
