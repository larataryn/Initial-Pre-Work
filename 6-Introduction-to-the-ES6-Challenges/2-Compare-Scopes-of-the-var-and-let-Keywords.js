/* Variables declared with var have a wider scope than variable declared with
let. let variables can also be restricted by blocks of code or expressions. */

function checkScope() {
"use strict";
  let i = "function scope";
  if (true) {
    let i = "block scope";
    console.log("Block scope i is: ", i);
  }
  console.log("Function scope i is: ", i);
  return i;
}
