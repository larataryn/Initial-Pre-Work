/* An arrow function can also be passed parameters by placing them in between
the parentheses. */

const myConcat = (arr1, arr2) => arr1.concat(arr2);
// test your code
console.log(myConcat([1, 2], [3, 4, 5]));
