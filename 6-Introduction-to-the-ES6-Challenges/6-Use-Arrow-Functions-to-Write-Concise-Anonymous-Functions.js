/* Arrow function syntax is used to create inline functions when the function
code doesn't need to be reused. It removes the function body and leaves only
a return statement. */

const magic = () => new Date();
