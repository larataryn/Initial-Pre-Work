/* The export default syntax is used to set a fallback value for a file, or if
only one value needs to be imported into another file. */

"use strict";
export default function subtract(x,y) {return x - y;}
